/** \brief me sirve para averiguar la suma de los dos numeros
 *
 * \param ingreso el numero uno
 * \param ingreso el numero dos
 * \return me va a devolver el resultado de la suma de los dos numeros
 *
 */
float suma (float primeroperando,float segundooperando);


/** \brief esta funcion me sirve para averiguar la resta de los dos numeros
 *
 * \param ingreso el numero uno
 * \param ingreso el numero dos
 * \return me va a devolver el resultado entre los dos numeros
 *
 */
float resta (float primeroperando,float segundooperando);


/** \brief Esta funcion me sirve para averiguar la multiplicacion entre los dos numeros
 *
 * \param ingreso el primer numero
 * \param ingreso el segundo numero
 * \return Me va a devolver el resultado de la multiplicacion de los dos numeros
 *
 */
float multiplicacion(float primeroperando,float segundooperando);


/** \brief Esta funcion me permite encontrar la division entre los dos numeros
 *
 * \param ingreso el primer numero
 * \param ingreso el segundo numero
 * \return Me da a devolver el resultado de la division de los dos numeros
 *
 */
float division(float primeroperando,float segundooperando);


/** \brief Me permite encontrar el factorial del primer numero
 *
 * \param primeroperando int ingreso el primer numero
 * \return int Me devuelve el factorial del determinado numero
 *
 */
int factorialuno(int primeroperando);


/** \brief Me permite encontrar el factorial del segundo numero
 *
 * \param segundooperando int Ingreso el segundo numero
 * \return int Me devuelve el factorial del segundo numero
 *
 */
int factorialdos(int segundooperando);
